{% extends "SuperjobsHomeBundle::layout.html.twig" %}


{% block container %}
	Veuillez remplir ce formulaire :


	<form role="form" action="{{ path('superjobs_home_recruiter_add') }}" method="post"  
    {{ form_enctype(form) }} >
            {# {{ tinymce_init() }} #}
            {{ tinymce_init({'use_callback_tinymce_init': true, 
                             'theme': {'simple': {'menubar': false}}})
                             }}
                             
{#         {{ form_errors(form) }}

        {{ form_row(form.name) }}
        {{ form_row(form.email) }}
        {{ form_row(form.subject) }}
        {{ form_row(form.body) }}

        {{ form_rest(form) }} #}
        {{ form_widget(form) }}

        <input type="submit" value="Submit" />
        
	<a href="{{ path('superjobs_home_recruiter_intro') }}">Annuler</a>
    </form>
{% endblock %}